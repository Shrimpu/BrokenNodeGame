using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class WorldData
{
    const float tickSpeed = 0.25f;

    public static float TickSpeed { get { return tickSpeed; } private set { } }

    public const float wireSkin = 0.08f;
}